# BGO Pipeline Workflow

On the birds eye view, the BGO pipeline consists of two steps:

- Preparation of input template files (class, individual, marker, abc atlas links): Driven by the Makefile in the `src/dendrograms` folder.
- Running the ODK pipeline to generate the ontology using the templates.

## Preparation of input template files

If you want to update a template (to reflect taxonomy changes probably), delete the relevant files and run `make` in the `src/dendrograms` folder. This will regenerate all the files.

Classes:
- `src/patterns/data/default/CCN20250428_class_base.tsv`: Generated by template_generation_tools.py `generate_base_class_template` function
- `src/patterns/data/default/CCN20250428_class_curation.tsv`: Automatically downloaded from the curation sheet (see below)

Markers:
- `CCN20250428_evidence_marker_set.tsv`: Generated by template_generation_tools.py `generate_evidence_marker_gene_set_template` function
- `CCN20250428_marker_set.tsv`: Empty in this pipeline
- `CCN20250428_nsforest_marker_set.tsv`: Empty in this pipeline
- `CCN20250428_within_subclass_marker_set.tsv`: Empty in this pipeline

Individuals:
- `src/templates/CCN20250428.tsv`: Generated by template_generation_tools.py `generate_ind_template` function

Atlas links:
- `src/dendrograms/CCN20250428_abc_urls*.json`: Generated by the Makefile in the `src/dendrograms` folder. See below "ABC Atlas links" for details.

Taxonomy:
- `src/dendrograms/CCN20250428.json`: Taxonomy file automatically downloaded from the BG taxonomy repo. Delete this file to refresh the taxonomy.


### Possible changes and actions to take:
- Taxonomy change: If the BG taxonomy has changed (https://github.com/brain-bican/basal_ganglia_consensus_taxonomy) and has a new CAS.json/rdf, delete all the files above and run `make` in the `src/dendrograms` folder.
- Curation Sheet changes:
  - HMBA_BGO_CCN20250428_class_curation: No need to delete templates, just run `make` in the `src/dendrograms` folder.
  - HMBA_BGO_one_concept_one_name_curation: Delete `src/patterns/data/default/CCN20250428_class_base.tsv` and `src/templates/CCN20250428.tsv` then run Makefile
  - HMBA_BGO_CL_ontology_subset: Delete all and run Makefile
  - BGCT_annotation: make a BG taxonomy release and then delete all templates and run `make` in the `src/dendrograms` folder.

## Running the ODK pipeline

In the `src/ontology` folder, run:
```bash
sh run.sh make prepare_release
```

Pipeline may generate error in the first run due to not  resolved entities in text definitions. This happens duw to a glitch in import/template generation timings. If the issue doesn't resolve in consecutive runs, try `clean` and repeat the prepare release.

```bash
sh run.sh make clean
```

### Curator Edit files:

Curation sheets are here: https://docs.google.com/spreadsheets/d/1yatMYkBVp7xac3DAJZ0r6jrtDBhyfjqAUcC1efDYLuc/edit?gid=0#gid=0
These are automatically downloaded in the dendrogram Make file (src/dendrograms/Makefile).

### ABC Atlas links
1. Update the kiwi URL payloads to configure the ABC Atlas links:
   - typescript/src/kiwi-payload.ts
2. To regenerate the ABC Atlas links, delete the following files:
   - src/dendrograms/CCN20230722_abc_urls.json
   - src/dendrograms/CCN20230722_abc_urls_evidence_marker_set.json
   - src/dendrograms/CCN20230722_abc_urls_marker_set.json
   - src/dendrograms/CCN20230722_abc_urls_nsforest_marker_set.json
   - src/dendrograms/CCN20230722_abc_urls_ws_marker_set.json
3. Then delete the DOSDP templates containing the ABC Atlas links:
   - src/patterns/data/default/CCN20230722_class_base.tsv
   - src/patterns/data/default/CCN20230722_evidence_marker_set.tsv
   - src/patterns/data/default/CCN20230722_marker_set.tsv
   - src/patterns/data/default/CCN20230722_nsforest_marker_set.tsv
   - src/patterns/data/default/CCN20230722_within_subclass_marker_set.tsv
4. Finally, run the following command in the `src/dendrograms` folder to regenerate the files:
```bash
make
```

## CL integration

Integration work is ongoing on branch https://github.com/obophenotype/cell-ontology/tree/basal_ganglia_component

This branch will ingest `bgo-cl-comp.owl` file and merge it to Cell Ontology.

## OLS publishing

https://cellular-semantics.sanger.ac.uk/ols4/ontologies/bgo

Local OLS (https://github.com/Cellular-Semantics/ols4) will read basal ganglia ontology from the `bgo.owl` on main branch. This configuration is here: https://github.com/Cellular-Semantics/ols4/blob/dev/dataload/configs/sanger.json

To refresh the BG ontology in the local OLS, follow the steps in the OLS documentation: https://github.com/Cellular-Semantics/ols4/blob/dev/hk_howto.md

1. Connect Server
2. Restart server
